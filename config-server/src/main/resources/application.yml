server:
  port: 8888

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/sameepmondhe/microservices-with-java/tree/main/config-server/src/main/configs